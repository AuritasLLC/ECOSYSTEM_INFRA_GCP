{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: front-asm-plus-config
  labels:
    app.kubernetes.io/name: front-asm-plus
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: document-management-frontend
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
